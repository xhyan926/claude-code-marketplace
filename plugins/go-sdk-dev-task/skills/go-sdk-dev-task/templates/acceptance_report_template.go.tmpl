# 子任务验收报告：{{ .TaskID }} - {{ .TaskName }}

**生成时间**：{{ .Timestamp }}

## 完成情况总结

### 实现内容
{{ .ImplementationSummary }}

### 解决的问题
{{- range .ProblemsSolved }}
- {{ . }}
{{- end }}

### 交付物
- [ ] 代码实现
- [ ] 单元测试
- [ ] 集成测试（如适用）
- [ ] 文档更新
- [ ] 示例代码（如适用）

## 测试结果详情

### 单元测试
- **测试用例总数**：{{ .TotalTestCases }}
- **通过数量**：{{ .PassedTests }}
- **失败数量**：{{ .FailedTests }}
- **通过率**：{{ .PassRate }}%
- **代码覆盖率**：{{ .CodeCoverage }}%

### 测试用例详情
| 测试用例名称 | 状态 | 备注 |
|--------------|------|------|
{{- range .TestCases }}
| {{ .Name }} | {{ if .Passed }}✓{{ else }}✗{{ end }} | {{ .Note }} |
{{- end }}

### 集成测试
| 场景 | 状态 | 备注 |
|------|------|------|
{{- range .IntegrationScenarios }}
| {{ .Name }} | {{ if .Passed }}✓{{ else }}✗{{ end }} | {{ .Note }} |
{{- end }}

### go-sdk-ut skill 使用情况
- **是否调用**：{{ .CalledUTSkill }}
- **测试用例数量**：{{ .UTTestCaseCount }}
- **测试覆盖率**：{{ .UTCodeCoverage }}%
- **所有测试是否通过**：{{ .AllTestsPassed }}

## 代码质量检查

### 静态代码分析
- [ ] **Go 代码规范检查**
  - `gofmt` 检查：{{ .GofmtResult }}
  - `golint` 检查：{{ .GolintResult }}
  - `go vet` 检查：{{ .GoVetResult }}

### 性能检查
- [ ] **内存使用**
  - 是否有内存泄漏：{{ .MemoryLeak }}
  - 大数据量测试：{{ .BigDataTest }}

- [ ] **响应时间**
  - 平均响应时间：{{ .AvgResponseTime }} ms
  - P99 响应时间：{{ .P99ResponseTime }} ms

### 错误处理
- [ ] **错误处理完善性**
  - 所有可能的错误路径是否覆盖：{{ .ErrorPathsCovered }}
  - 错误信息是否清晰：{{ .ErrorMessagesClear }}
  - 错误日志是否完整：{{ .ErrorLogsComplete }}

## 文档和示例

### 代码注释
- [ ] 公开 API 有完整的 GoDoc 注释
- [ ] 复杂逻辑有行内注释
- [ ] 常量和变量命名清晰

### 示例代码
- [ ] 示例代码已更新
- [ ] 示例代码可运行
- [ ] 示例输出符合预期

```go
// 示例代码运行结果：
{{ .ExampleOutput }}
```

## 兼容性和破坏性变更

### 兼容性
- [ ] 向后兼容：{{ .BackwardCompatible }}
- [ ] 现有测试仍通过：{{ .ExistingTestsPass }}
- [ ] 不影响现有用户：{{ .NoUserImpact }}

### 破坏性变更
{{ .BreakingChanges }}

## 性能对比

| 指标 | 修改前 | 修改后 | 变化 |
|------|--------|--------|------|
{{- range .PerformanceMetrics }}
| {{ .Metric }} | {{ .Before }} | {{ .After }} | {{ .Change }} |
{{- end }}

## 已知问题和限制

{{- range .KnownIssues }}
1. {{ . }}
{{- end }}

## 改进建议

### 代码层面
{{- range .CodeImprovements }}
1. {{ . }}
{{- end }}

### 测试层面
{{- range .TestImprovements }}
1. {{ . }}
{{- end }}

### 文档层面
{{- range .DocImprovements }}
1. {{ . }}
{{- end }}

## 验收结论

### 评审结果
- [ ] 通过
- [ ] 需要修改
- [ ] 需要返工

### 评审意见
{{ .ReviewComments }}

### 后续行动
{{- range .NextActions }}
- {{ . }}
{{- end }}

---

**评审人**：{{ .ReviewerName }}
**评审日期**：{{ .ReviewDate }}
